# 最新状态覆盖（2026-07-30）

> **本节优先级高于下方旧交接正文。** 下方旧路径、旧阶段状态和“一次性完成”安排仅保留作历史设计依据。

- **阶段 4.5 已完整收口**：十一来源验收通过；`GD_Phase45AutomatedTests.cs` 已删除；清理后 Release 构建、白名单同步和 DLL 哈希记录完成。
- **阶段 5 已完整收口**：FRD 可选反射联动、FCD 独立回归、软降级、不重选最终身份和随机基因生成修复全部通过游戏内验收。
- 原五项总测试为通过 5、失败 0、未执行 0；随机基因回归为生成 20、清理 20、随机位点 24、成功新增 24、异常 0。
- `GD_Phase5AutomatedTests.cs` 已删除；最终源码/DLL 中阶段 5 临时按钮字符串均不存在；只保留六个长期诊断按钮。
- 阶段 5 最终 Release DLL 已同步，SHA-256 为 `83FA51F6F9B9575F863B1D423236EA980533D857CC90CEA7BD66288FCFDA82A3`。
- **阶段 6 未开始。** 后续会话应以当前项目 `AGENTS.md`、`project.yaml` 和阶段 5 最终报告为准，不得把下方旧正文中的阶段状态当成当前事实。

---
# 基因多样性模组阶段 4～6 一次性完成交接文档

> 日期：2026-07-28  
> 模组：基因多样性（Genetic Diversity）  
> PackageID / Harmony ID：`yyyyy.geneticdiversity`  
> 目标版本：RimWorld 1.6  
> 最后更新：2026-07-29  
> 当前结论：阶段 1～4 的游戏内日志验收已完成。阶段 4 三组合合并明确失败为 0；绮罗族缺固定边界标记但由直接值和总汇总支持带限定通过。`GD_Phase4AutomatedTests.cs` 已删除，六个永久诊断按钮保留；清理后 Release 构建、白名单同步与新哈希均待主会话完成。现有摘录不是完整 `Player.log`，不能宣称完整启动无红字。阶段 5、6 仍未实现，不得提前实施阶段 4.5/5/6 规则。

---

## 0. 新会话开工前必须读取

1. `C:\Users\123456\.cc-switch\skills\rimworld-mod-development\SKILL.md`
2. `C:\Users\123456\.cc-switch\skills\rimworld-mod-development\references\一键测试方案模板.md`
3. `D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity\AGENTS.md`
4. `D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity\模组文档\核心设计公约.md`
5. `D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity\模组文档\模组修改注意事项.md`
6. `D:\RimWorldModding\模组制作工作台\方案相关\基因多样性模组详细设计方案_2026-07-27.md`
7. `D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity\测试\阶段3游戏内验收记录_2026-07-28.md`

先按《Rimworld 模组创作之书》索引查资料和避坑集，不得跳过。

---

## 1.1 阶段 4 当前交付状态（2026-07-28）

已完成：

- `GD_RaceGeneCompatibilityDef.cs` 与 `GD_CompatibilityRegistry.cs`；
- Kiiro、Milira、Wolfein、Ratkin Gene Expanded、OA Ratkin、Epona/Unicorn/Destrier/两个 Milira 混血 Race 的精确兼容 Def；
- 结构/自定义 geneClass 同种族边界、Wolfein 纯血排除、OA / `OAGene_*` 普通与生化异种类型可遗传边界；
- 历史测试版曾提供 12 个阶段 4 只读中文 Debug Action；
- `测试/阶段4用户测试原始日志_2026-07-28.txt` 已按来源原样保存，来源/副本 SHA-256 均为 `27EF95012AF94ED9FA2683351EB88C543EE3158E817097FD52795C9EB4F0BDFF`；
- Agent 已填写 `测试/阶段4游戏内验收记录_2026-07-28.md`：三组合合并失败=0，阶段 4 通过；绮罗族为带限定通过；
- `GD_Phase4AutomatedTests.cs` 已删除，六个永久诊断按钮保留；
- 阶段 4 含按钮历史测试版开发/运行 DLL SHA-256：`88416386C00DF3BD2AA5AA9F67AF73E800D078240271041C7AEFB65C4621E0F0`。

待主会话完成：

1. 对清理后的源码执行 Release 构建；
2. 从开发主副本向运行副本执行白名单同步；
3. 记录清理后开发/运行 DLL SHA-256，并扫描最终程序集确认无阶段 4 临时按钮；
4. 完成上述构建同步后再进入后续阶段。本交接文档不授权提前实现阶段 4.5、5 或 6。

---
## 1.2 双副本与阶段 3 清理基线

开发主副本：

`D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity`

运行副本（RimWorld 加载与本地测试使用的 Mods 根最小副本，迁移后仍保留）：

`D:\steam\steamapps\common\RimWorld\Mods\yyyyy_GeneticDiversity`

阶段 3 清理后 DLL SHA-256：

`E3B826924866267D9DE2D942C61783469126100F8F30F26146F9ABC7C09F12D0`

阶段 3 清理时运行副本只包含：

- `About/About.xml`
- `1.6/Assemblies/GeneticDiversity.dll`

阶段 3 清理后永久保留的开发者诊断按钮只有：

- `清空基因池缓存`
- `立即刷新基因池缓存`
- `只读统计当前地图成年人物`
- `只读查看第三阶段HAR适配器报告`
- `只读查看世界系谱池按种族摘要`
- `只读查看第二阶段出生诊断`

第三阶段测试专用按钮与 `GD_Phase3AutomatedTests.cs` 已在日志判定完成后删除；阶段 4 的 `GD_Phase4AutomatedTests.cs` 也已按相同生命周期删除。当前永久保留按钮仍只有上述六个。后续每个阶段继续遵守：能一键的全部一键化，日志由 Agent 判定和填表，本阶段记录完成后立即删除本阶段测试入口，再继续后续开发。

---

## 2. 阶段 1～3 已完成状态

### 阶段 1

- 新生成非新生儿人物 0～3 个额外系谱基因位点；
- 20%/55%/20%/5%；
- 每个位点 90% 当前世界常见池、10% 突变池；
- 冲突、前置、代谢、暴力能力和核心身份保护；
- 用户已验收通过。

### 阶段 2

- 真正出生时 10% 单基因突变；
- 不修改原始 GeneSet；
- 不调用 `GetInheritedGenes`；
- 原版 Human 边界保持；
- 用户已验收通过。

### 阶段 3

- HAR 无硬依赖反射适配；
- 当前世界 HAR 系谱基因进入种族/阵营来源快照；
- 同种族同阵营 ×4、同种族不同阵营 ×2、其他种族普通基因 ×1；
- 种族固有基因与异种类型回退；
- 自定义/结构类基因同种族边界；
- HAR 最终 AddGene 拒绝；
- 用户日志总体通过。

第三阶段正式验收记录：

`D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity\测试\阶段3游戏内验收记录_2026-07-28.md`

原始日志副本：

`D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity\测试\阶段3用户测试原始日志_2026-07-28.txt`

阶段 3 不再重复开发，除非阶段 4～6 引起回归。

---

## 3. 永久核心原则

1. 当前存档实际人物的系谱基因永远高于 Def 回退；
2. 原版 Human 与 HAR 人形异族地位相同；
3. 不删除或替换种族、异种类型、种族固有基因和任务核心基因；
4. 不把异种基因批量转成系谱基因；
5. 不放宽冲突、前置、代谢、暴力能力和 HAR 限制凑位点；
6. 候选非法时少加或不加；
7. 可选兼容全部使用 XML 条件或一次性反射，不产生第三方程序集硬依赖；
8. 玩家可见按钮、设置、消息、日志和测试文档使用查证过的准确简体中文。

---

# 第一部分：阶段 4——已知 HAR 基因模组精确兼容

## 4. 阶段 4 总目标

为已核对的六类来源建立精确的种族、异种类型、结构基因和自定义基因传播边界，不能只依靠阶段 3 通用启发式。

建议新增：

- `GD_RaceGeneCompatibilityDef.cs`
- `GD_CompatibilityRegistry.cs`
- `1.6/Defs/Compatibility/*.xml`

建议 Def 字段至少包含：

```text
raceDefName
nativeXenotypeDefNames
nativeGeneDefNames
excludedGeneDefNames
sameRaceOnlyGeneDefNames
crossRaceSafeGeneDefNames
excludedPawnKindDefNames
```

要求：

- 字符串在 Def 解析阶段用安全方式解析；
- 缺少来源模组时静默跳过；
- XML 使用 `MayRequire` 或等价来源包条件；
- 不复制第三方 GeneDef；
- 不修改第三方模组文件；
- 新增 Def 后必须使用 `rimworld-source-def-index` 刷新本模组 Wiki，并为每条 Def 补准确中文“游戏解释”。

## 5. 精确兼容顺序与规则

### 5.1 Kiiro Race Gene Patch

- packageId：`Ancot.KiiroRaceGenePatch`
- 本地源码：`模组制作工作台/创意工坊其他模组及源码/HAR系列/已兼容/Kiiro Race Gene Patch`
- `KiiroXenotype` 可作为绮罗族常见来源；
- 以普通基因为主；
- 不能把 Baseliner 全部基因误当绮罗族核心；
- 跨种族仍经过 HAR 与通用安全规则。

### 5.2 Milira Race Gene Patch

- packageId：`Ancot.MiliraRaceGenePatch`
- 本地源码：`.../HAR系列/已兼容/Milira Race Gene Patch`
- 基础 Milira 使用其专属异种类型；
- Epona-Milira 混血必须使用自己的 `Xeno_EponaMilira` / `Xeno_UnicornMilira`；
- 不得把基础 Milira、基础 Epona 和混血池合并。

### 5.3 Wolfein Race Gene Patch

- packageId：`Ancot.WolfeinRaceGenePatch`
- 本地源码：`.../HAR系列/已兼容/Wolfein Race Gene Patch`
- 可遗传沃芬异种类型可进入同种族常见池；
- 纯血不可遗传异种类型不得自动转成系谱来源；
- 月相、夜间适应、肉食偏好、低血量机制等自定义基因类默认只在 Wolfein 种族内传播；
- 跨种族放行必须逐类源码审查后显式列入。

### 5.4 Ratkin Gene Expanded

- packageId：`EoralMilk.RatkinGeneExpanded`
- 本地源码：`.../HAR系列/已兼容/Ratkin Gene Expanded`
- 鼠耳、鼠尾、鼠族身体等结构基因只在 Ratkin 种族内；
- 工作、体质、代谢类标准基因在 HAR 允许时才可跨种族；
- 只读取实际加载的 1.6 内容，禁止把旧版或 `1.6_unofficial` 重复计入。

### 5.5 [OA] Ratkin Gene Expand

- packageId：`OARK.RatkinFaction.GeneExpand`
- 本地源码：`.../HAR系列/已兼容/[OA]Ratkin Gene Expand`
- 自定义基因类、身体部位、Hediff、睡眠、造血、灵感和服从逻辑默认只在 Ratkin 种族内；
- 即使同种族也必须经过 HAR、冲突、前置和代谢检查；
- 来源未加载时不保留字符串伪引用。

### 5.6 Epona Dynastic Rise

- packageId：`Epona.EponaDynasticRise`
- 本地源码：`.../HAR系列/已兼容/Epona Dynastic Rise`
- 基础 Epona、Unicorn、Destrier 与两个 Epona-Milira 混血异种类型分别建池；
- 尊重其 `ApplyBirthOutcome` Postfix 和 `NewbornXenotypeOverride`；
- 本模组不在出生 Prefix 设置异种类型；
- 不破坏 Epona 出生后的身份修正。

---

# 第二部分：阶段 5——与前两个多样性模组联动

## 6. 阵营种族多样性联动

开发主副本：

`D:\RimWorldModding\临时存放\yyyyy_MixedPeoplesFactions`

packageId：`yyyyy.mixedpeoplesfactions`  
Harmony ID：`yyyyy.factionracediversity`

建议新增：

- `GD_FrdAdapter.cs`

必须通过可选反射读取现有公开服务/设置：

- `FRD_RaceRegistry.HumanlikeRaces`
- `FRD_RaceRegistry.GetKinds(race)`
- `FRD_XenotypeService.GetAllowedXenotypes(race)`
- `FRD_XenotypeService.IsSafeForRace(...)`
- `MPF_Mod.Settings`
- `MPF_Settings.GetFactionSettings(...)`
- 阵营 `raceWeights`
- 各种族异种类型权重

规则：

1. 阵营种族多样性先选择最终种族、人物种类和异种类型；
2. 基因多样性不得重新选择或覆盖；
3. 权重大于 0 的配置只作为“当前世界没有样本时”的低权重先验；
4. 当前存档实际人物系谱仍拥有最高优先级；
5. 未加载 FRD 时退回阶段 3 逻辑；
6. 反射失败只警告一次，不阻止人物生成；
7. 如现有公开成员不足，可给 FRD 增加稳定只读 Integration API，但必须同时维护 FRD 双副本、文档、构建和回归；不能直接读取脆弱私有字段后不做版本检查。

Harmony 顺序必须保证：

```text
阵营种族多样性选择种族/人物种类/异种类型
→ 原版与 HAR 安装核心基因
→ 基因多样性追加安全变体
```

## 7. 阵营文化多样性回归

开发主副本：

`D:\RimWorldModding\临时存放\yyyyy_FactionCulturalDiversity`

packageId：`yyyyy.factionculturaldiversity`

阶段 5 不建立“文化决定基因”或“基因决定文化”的自动绑定。

必须验证：

- 人物种族和异种类型由 FRD/原版决定；
- 基因差异由本模组追加；
- 个人文化可以独立变化；
- 政治阵营不被改变；
- 三模组同时启用时保存读取正常；
- 不形成事实硬依赖。

---

# 第三部分：阶段 6——设置、统计与旧档工具

## 8. 设置系统

建议新增：

- `GD_Mod.cs`
- `GD_Settings.cs`
- 必要的设置窗口辅助类

至少实现以下设置：

1. 启用基因多样性；
2. 玩家阵营新人物：开/关；
3. NPC 新人物：开/关；
4. 多样性强度：保守 / 标准 / 高；
5. 突变比例，默认 10%；
6. 新生儿突变几率，默认 10%，每次最多 1 个基因；
7. 允许普通功能基因跨种族，默认开启；
8. 允许外观/身体/自定义基因跨种族，默认关闭；
9. 详细日志，默认关闭；
10. 只使用同种族基因池；
11. 按阵营设置强度；
12. 按种族设置强度；
13. 是否允许不可遗传异种类型的基因进入自然突变；
14. 是否允许超凡突变；
15. 缓存立即失效/刷新入口。

要求：

- 默认值保持当前阶段 1～3 已验收行为；
- 设置修改后立即失效世界池/目标池缓存；
- 所有中文从原版语言与本模组固定术语表核对；
- 设置页只解释玩法，不出现“按用户要求”“测试需要”等内部措辞。

## 9. 统计页面

至少提供：

- 当前世界扫描人物数；
- 种族数；
- 系谱基因记录数；
- 同种族/跨种族/Def 回退添加数；
- HAR 拒绝数；
- 缓存刷新耗时；
- 按种族的常见基因摘要；
- 按来源模组的基因数量；
- 阶段 4 精确兼容命中数量；
- FRD 先验命中数量。

统计页面是玩家可见功能，使用准确中文；不得要求玩家读内部英文变量。

## 10. 旧档手动补充工具

必须是玩家主动操作，默认绝不自动回填。

建议提供范围：

- 仅选中人物；
- 仅玩家阵营；
- 仅当前地图；
- 可选全世界（强警告）。

必须：

- 点击前二次确认；
- 要求另存测试档；
- 跳过婴儿、任务专用人物、无基因追踪器人物；
- 复用正式候选与安全校验；
- 保存已处理 `thingIDNumber`，防止每次加载重复追加；
- 提供查看处理数量和清除处理记录的安全入口；
- 新增 WorldComponent 后重新评估移除兼容，并在 About/发布说明中准确说明；
- 不直接修改存档 XML。

---

# 第四部分：一次性实施顺序

## 11. 禁止拆成多个后续阶段

新会话按以下顺序连续工作，不在阶段 4 完成后停止交接：

1. 记录当前阶段 3 清理后源码与 DLL 哈希；
2. 核对六个 HAR 基因来源的真实 Def、源码、packageId 和中文名；
3. 实现精确兼容 Def 类型、注册表和 XML；
4. 构建并做静态检查；
5. 实现 `GD_FrdAdapter` 与三模组加载/Harmony 顺序；
6. 必要时给 FRD 增加稳定只读 API，并同步维护其双副本；
7. 实现设置页；
8. 实现统计页面；
9. 实现旧档手动补充工具及处理记录；
10. 更新 About、翻译、四件套、Wiki 和创作之书；
11. 按模板建立阶段 4～6 一键测试方案；
12. 添加临时 `GD_Phase4To6AutomatedTests.cs` 与阶段总测试按钮；
13. Release 构建并同步运行副本；
14. 用户只点击按钮并提供日志；
15. Agent 自动读取日志、填写表格、判断结果；
16. 修复失败项并复测；
17. 全部必要测试通过后删除测试专用按钮/辅助类；
18. 最终 Release 构建、双副本同步、哈希核对和发布前清理。

---

# 第五部分：阶段 4～6 测试强制要求

## 12. 一键测试体系

测试方案必须基于：

`C:\Users\123456\.cc-switch\skills\rimworld-mod-development\references\一键测试方案模板.md`

至少包含临时按钮：

### 阶段 4

- 一键检查六个精确兼容来源是否检测；
- 每个来源一个独立边界测试；
- 一键检查结构基因不会跨种族；
- 一键检查自定义基因类不会跨种族；
- 一键检查 Epona-Milira 池不混淆；
- 一键检查来源卸载软降级。

### 阶段 5

- 一键检查 FRD 适配器状态；
- 一键构造阵营配置先验并验证“世界实际样本 > FRD 先验”；
- 一键验证最终种族/人物种类/异种类型不被本模组重选；
- 一键执行三模组联合内存测试；
- 一键检查未加载 FRD/FCD 时软降级。

### 阶段 6

- 一键检查所有默认设置与阶段 3 行为一致；
- 一键检查设置修改后的缓存失效；
- 一键检查玩家/NPC 开关；
- 一键检查三档强度；
- 一键检查突变比例与出生概率；
- 一键检查统计页面数字来源；
- 一键创建旧档补充临时样本；
- 一键验证重复处理保护；
- 准备/验证保存读取；
- 一键检查移除安全结构。

能安全串行的只读项目再提供：

`一键执行阶段4至6全部只读测试`

每个按钮必须生成带开始/结束边界、步骤理由、实际值、预期值和最终结论的中文日志；测试方案还必须逐按钮写清“一键”自动执行的每一步、实际调用/构造对象，以及为什么该步骤能验证目标。阶段 4、5、6 都要先建立“测试项目→一键入口”映射，能安全自动化的项目不得留给用户手工完成。

用户只负责点击并提供日志文件。Agent 负责：

- 读取日志；
- 判断结果；
- 复制关键日志；
- 填写全部表格；
- 更新验收记录；
- 删除通过后的测试按钮；
- 最终重建与同步。

---

# 第六部分：完成标准

## 13. 功能完成

- 六个 HAR 基因来源均有精确规则；
- 自定义/结构基因传播边界有明确 Def，不依赖猜测；
- FRD 可选联动完成，世界实际系谱仍最高优先；
- FCD 联合使用不互相绑定或覆盖；
- 设置页全部实现且默认行为不回归；
- 统计页面可用；
- 旧档工具安全、手动、可防重复；
- 不增加不必要硬依赖。

## 14. 测试完成

- 阶段 4～6 所有必要按钮日志已由 Agent 判定；
- 测试表格和验收记录由 Agent 自动填写；
- 用户没有被要求人工计算或复制表格；
- 失败项修复并复测；
- 每个阶段的测试专用按钮在该阶段日志判定和记录完成后立即删除，不拖到所有阶段结束；
- 最终只保留长期玩家功能和必要诊断按钮。

## 15. 发布完成

- Release 0 warning、0 error；
- 开发/运行 DLL 和 About 哈希一致；
- 运行副本保持最小白名单；
- 新 Def Wiki 与游戏解释完整；
- 四件套、测试记录、修改日志、交接文档与创作之书索引同步；
- 最终回复列出功能、日志结论、删除的测试按钮、保留的永久按钮、最终哈希和仍存限制。

---

## 16. 可直接复制给新会话的任务提示词

> 请使用 `$rimworld-mod-development` 技能，一次性完成 `D:\RimWorldModding\临时存放\yyyyy_GeneticDiversity` 的剩余阶段 4、阶段 5、阶段 6，不要在单一阶段结束后再次交接。先完整阅读 `D:\RimWorldModding\模组制作工作台\其他文档\基因多样性模组阶段4至6一次性完成交接文档_2026-07-28.md`、详细设计方案、项目 AGENTS/核心公约/修改注意事项，以及技能内 `references\一键测试方案模板.md`。阶段 1～3 已验收通过，阶段 3 清理后 DLL SHA-256 为 `E3B826924866267D9DE2D942C61783469126100F8F30F26146F9ABC7C09F12D0`，测试专用按钮已删除，只保留六个永久诊断按钮。请连续实现六个已知 HAR 基因来源精确兼容、阵营种族多样性可选反射联动、阵营文化多样性回归、全部设置、统计页面和旧档手动补充工具。所有能安全自动化的测试必须做成准确中文一键 Debug Action，能安全串行的无污染项目必须有阶段总按钮；测试方案逐条解释“一键”做了什么、为什么能验证，并生成带步骤理由和最终结论的完整日志。用户只点击并提供日志，表格和判定由 Agent 完成。每个阶段日志判定并记录完成后立即删除该阶段测试专用按钮/辅助类、重建并核对，最后再做阶段 4～6 总体 Release 构建、双副本同步、哈希核对、Wiki/四件套/创作之书更新。不得硬引用 HAR、FRD、FCD 或任何可选基因模组 DLL，不得修改游戏本体，不得降低当前世界系谱优先和 Race 安全规则。



