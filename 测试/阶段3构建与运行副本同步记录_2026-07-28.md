# 基因多样性：阶段 3 构建与运行副本同步记录

> 日期：2026-07-28  
> 当前状态：Release 构建与运行副本白名单同步已完成。

## 1. Release 构建

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1 -Configuration Release
```

结果：

- 0 warning；
- 0 error；
- 输出：`1.6/Assemblies/GeneticDiversity.dll`；
- 开发主副本 DLL SHA-256：`5E83E461CA86A3B1462C8AF2934BBC19ED46D7ADAB056388E28AB38512BCB2E2`；
- 开发主副本 About.xml SHA-256：`5BD2AC4B889AB0CE39324BF7A1D921A93A6FD202BFDBCDCED422D1801155396C`。

完整输出：`阶段3最终Release构建输出_2026-07-28.log`。

## 2. 同步安全检查

首次同步时检测到：

- `RimWorldWin64.exe` 正在运行；
- 运行副本 DLL 仍为阶段 2 哈希 `8ADC84CD982C0B4BC7ACD4ADC48EDCFEC25C96878497B7388BB8D85FB09A0C0D`；
- 运行副本文件白名单仍正常，仅含 About.xml 与 DLL。

按强制规则未结束用户游戏、未强行覆盖被映射 DLL。

已创建并启动：

`阶段3等待游戏退出后同步运行副本.ps1`

该脚本只等待 `RimWorldWin64` 正常退出，不会终止进程；退出后白名单复制 About.xml 与 DLL，核对两份 SHA-256，并写入 `phase3_runtime_sync_2026-07-28.log`。

## 3. 最终同步结果

- 状态：等待 RimWorld 正常退出后，于 2026-07-28 14:05:31（Asia/Shanghai）自动同步成功；
- 最终开发/运行 DLL SHA-256：`5E83E461CA86A3B1462C8AF2934BBC19ED46D7ADAB056388E28AB38512BCB2E2`；
- 最终开发/运行 About.xml SHA-256：`5BD2AC4B889AB0CE39324BF7A1D921A93A6FD202BFDBCDCED422D1801155396C`；
- 运行文件清单：`About/About.xml`、`1.6/Assemblies/GeneticDiversity.dll`；
- 同步日志：`phase3_runtime_sync_2026-07-28.log`。

## 4. 测试入口易用性修订

- 根据用户反馈新增准确中文按钮 `一键测试系谱基因四比二比一权重`；
- 取消手工准备三组 Pawn、编辑阵营/基因、生成 200 人与人工计算；
- 重新 Release 构建并同步，最终开发/运行 DLL SHA-256：`5E83E461CA86A3B1462C8AF2934BBC19ED46D7ADAB056388E28AB38512BCB2E2`。




## 5. 全套按钮化测试修订

- 新增 `GD_Phase3AutomatedTests.cs` 与全套准确中文一键测试按钮；
- 阶段三白话测试方案已全部重写为逐按钮操作；
- Release 构建 0 warning、0 error；
- 最终开发/运行 DLL SHA-256：`5E83E461CA86A3B1462C8AF2934BBC19ED46D7ADAB056388E28AB38512BCB2E2`。


## 6. 验收后测试按钮清理

- 用户日志已由 Agent 判定并写入正式验收记录；
- 已删除第三阶段测试辅助类和测试专用按钮；
- 最终保留六个长期诊断按钮；
- 清理后 Release 构建 0 warning、0 error；
- 清理后最终开发/运行 DLL SHA-256：`E3B826924866267D9DE2D942C61783469126100F8F30F26146F9ABC7C09F12D0`。

