# 阶段 5 静态检查清单

> 日期：2026-07-30  
> 状态：原五项联动测试与随机基因生成回归全部通过；阶段 5 临时测试源码已删除，最终 Release 构建、同步、`sync-verify` 与字符串扫描完成。

## 1. 配置与结构

- [x] `workbench.py validate --strict`：errors=0，warnings=0，退出码 0。
- [x] `project.yaml` 仍为 schema v2；开发/运行目录边界正确。
- [x] `About.xml` packageId 仍为 `yyyyy.geneticdiversity`。
- [x] FRD/FCD 仅加入 `loadAfter`，未加入 `modDependencies`。
- [x] 运行 XML 共 5 个，解析错误 0。

## 2. 阶段 5 代码契约

- [x] 新增 `GD_FrdAdapter.cs`；通过 `GenTypes.GetTypeInAnyAssembly` 与公开反射读取：`FRD_RaceRegistry.HumanlikeRaces`、`GetKinds`、`FRD_XenotypeService.GetAllowedXenotypes/IsSafeForRace`、`MPF_Mod.Settings`、`MPF_Settings.GetFactionSettings` 和阵营/种族权重字段。
- [x] C# 与 csproj 中 `MixedPeoplesFactions` / `FactionCulturalDiversity` 直接 using/程序集 Reference 数量为 0。
- [x] FRD 未加载或反射失败时返回空先验；失败只警告一次，不阻止人物生成。
- [x] FRD 先验只在 `!hasTargetRaceSample` 分支加入，总权重常量为 0.5。
- [x] 世界实际观察仍按同阵营同 Race ×4、同 Race ×2、其他 Race 标准基因 ×1；不会被 FRD 先验覆盖。
- [x] HAR 缺失但 FRD 可用时，Human 目标仍会进入目标池逻辑，不会因 `snapshot.HarAware=false` 跳过 FRD。
- [x] FRD 先验仍经过精确兼容、HAR CanHaveGene、正式候选冲突/前置/代谢/暴力安全链。
- [x] `GD_Patch_PawnGenerator_GenerateGenes` 声明 HarmonyAfter HAR 与 `yyyyy.factionracediversity`。
- [x] 适配器不接收或修改 `PawnGenerationRequest`，不重选 Race、PawnKind、Xenotype 或 Faction。
- [x] FCD 只读取是否加载状态用于联合回归，不读取文化/意识形态作为基因权重。

## 3. 自动化测试入口

- [x] 历史测试版 `GD_Phase5AutomatedTests.cs` 共提供 7 个 Debug Action；完成验收后已删除。
- [x] 阶段总按钮：`一键执行阶段5当前组合全部只读测试`。
- [x] 其余按钮覆盖 FRD 状态、正权重先验/世界样本优先、不重选最终身份、三模组联合内存测试、缺失来源软降级。
- [x] 日志包含固定开始/结束边界、自动步骤、实际值、预期值、失败原因、最终结论。
- [x] 测试版源码连续问号占位扫描为 0；最终源码和 DLL 中阶段 5 临时按钮与 `GD_Phase5AutomatedTests` 均不存在。
- [x] 阶段 4.5 测试源码不存在；源码和 DLL 中 `GD_Phase45AutomatedTests` 均不存在。

## 4. 构建、Wiki 与同步

- [x] Release 构建：0 warning，0 error，退出码 0。
- [x] 原阶段 5 联动测试版 DLL：82944 字节，SHA-256 `5DF0A81101A1FA0EDF8E5667956E62F23E78CF65BFB747E6C89BEF7C3F6BCDEB`。
- [x] 随机基因恢复修复版 DLL：87552 字节，SHA-256 `EC7C2E1749B6F799AB389429566944E75965D68B4F97A8696D266520CCD8A24B`。
- [x] Wiki mixed 重建：Def 文件 3、条目 22、翻译文件 2、翻译键 22；重复/空值/冲突均为 0；人工 Markdown 保留 2。
- [x] 白名单同步：运行副本 7 文件；开发/运行逐文件 SHA-256 全部一致；无 Source、Wiki、测试或文档污染。

## 5. 阶段 2 红线回归

- [x] `GD_BirthMutationPatch.cs`：`2781ED2166047689D5A3343B006F80499252A8615C85618D7C6D1EDFB7FD1374`，与既有基线一致。
- [x] `GD_BirthMutationUtility.cs`：`43D9A903825E8CAC086C74FA270DB0297370E8BF53164D83027D4E259B596F91`，与既有基线一致。
- [x] `GD_MetabolismUtility.cs`：`85187E1B12181A43EBA3761866B113D19BDDF956D88515D950E4462B7B834804`，与既有基线一致。

## 6. 2026-07-30 原联动游戏内验收

- [x] 完整 `Player.log` 同轮加载 GD、FRD、FCD。
- [x] FRD 状态、FRD 先验/世界样本优先、不重选 Race/PawnKind/Xenotype、三模组联合内存测试、软降级契约全部通过。
- [x] 阶段总按钮：通过 5、失败 0、未执行 0、最终结论通过。
- [x] 工作台 `accept`：freshness=true、same_session=true、related_errors=0。
- [x] 原日志 SHA-256：`DB88068970BF41A9D34E8BCDB232357636D5090E8F2D48662A7B6E229A8906F8`；验收记录与证据目录已保存。

## 7. 随机基因恢复修复

- [x] 根因候选：Common 主池非空但全部候选对新 Pawn 不合法时，旧逻辑在 20 次候选耗尽后直接放弃位点；旧日志没有真实 Pawn 生成计数，因此该项是代码审查结合用户现象的判断，不伪称日志直接证明拒绝次数。
- [x] `GD_WorldGenePool.cs` 新增目标 Race 受限恢复池；`GD_GeneSelector.cs` 仅在主池失败后尝试，Mutation 路径不变。
- [x] 恢复候选继续经过精确兼容、HAR `CanHaveGene`、重复、冲突、前置、代谢、暴力能力与 `AddGene` 最终裁决。
- [x] `GD_Diagnostics.cs` 新增恢复池尝试/成功计数。
- [x] 新按钮临时生成 20 个成年 Colonist，走正式 FRD→GenerateGenes→GD 链路，统计并清理全部测试 Pawn。
- [x] 修复后严格校验：0 error、0 warning；XML 5 文件错误 0；Debug Action 共 7；连续 `???` 占位 0。
- [x] Release 构建：0 warning、0 error；sync 预览只复制 DLL、prune=0；`sync --apply` 成功；`sync-verify` 7 文件一致；运行污染 0。

## 8. 随机基因修复游戏内验收

- [x] 用户手动重启 RimWorld 并载入修复版 DLL。
- [x] 同轮加载 GD、FRD、FCD；工作台 `accept` 为 PASS，`related_errors=0`。
- [x] 回归按钮：生成=20、清理=20、GenerateGenes 调用增量=20、合格人物增量=20。
- [x] 随机位点增量=24、成功新增基因=24、异常=0、最终结论通过。
- [x] 本批主池候选已合法，恢复池尝试=0、成功=0；不影响随机基因正式生成链路恢复的结论。
- [x] 日志 SHA-256：`B721F3946D29D23729A18F9FFAA171171F11E145267E3B217E0DD42ED372ABDB`。

## 9. 最终清理与收口

- [x] 删除 `Source/GeneticDiversity/GD_Phase5AutomatedTests.cs`；删除前 SHA-256：`1E4FF938CDAEF4A7D963106271AFE9261905A67DA99A4DF46E106687D49EE4DB`。
- [x] 严格校验：errors=0、warnings=0、退出码 0。
- [x] Release 构建：0 warning、0 error、退出码 0。
- [x] sync 预览：copy=1、skip=6、prune=0；`sync --apply` 成功，未使用 `--prune`。
- [x] `sync-verify`：7 文件一致，optional_missing=0；运行副本污染 0。
- [x] 最终源码中阶段 5 临时类/按钮命中 0；最终 DLL 的八个阶段 5 临时类/按钮字符串均不存在。
- [x] 只保留六个长期诊断 Debug Action。
- [x] 最终开发/运行 DLL 均为 75776 字节，SHA-256 `83FA51F6F9B9575F863B1D423236EA980533D857CC90CEA7BD66288FCFDA82A3`。
- [x] 阶段 5 完整收口；阶段 6 尚未开始。

