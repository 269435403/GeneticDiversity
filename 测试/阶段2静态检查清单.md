# 基因多样性：阶段 2 静态检查清单

> 日期：2026-07-28  
> 执行者：Agent  
> 说明：本清单主体记录 Agent 完成的静态/构建检查；其后游戏内项目已由用户于 2026-07-28 反馈全部通过。

## 1. 设计与边界

| 检查项 | 结果 | 证据 |
|---|---|---|
| 当前存档异种 Endogenes 优先被固化为永久核心公约 | 通过 | 根目录 `AGENTS.md`、`模组文档/核心设计公约.md`、交接文档、详细设计方案 |
| 原版与 HAR 在长期公约中同等适用 | 通过 | 公约明确阶段 3 必须纳入当前存档 HAR Pawn Endogenes |
| 阶段 2 未提前实现 HAR/设置/旧档回填 | 通过 | 新代码只处理 ApplyBirthOutcome 与原版 Human 双亲 |
| 不改写父母遗传规则 | 通过 | Source 中无 `GetInheritedGenes` 引用 |
| 不新增自定义 Def/存档字段 | 通过 | 无 Defs、Scribe、WorldComponent、GameComponent 新增 |

## 2. Harmony 接入

| 检查项 | 结果 | 证据 |
|---|---|---|
| 精确绑定 ApplyBirthOutcome 11 参数签名 | 通过 | `GD_BirthMutationPatch.cs` Type[] 与本地 RimWorld 1.6 `PregnancyUtility.cs` 一致 |
| 使用 Prefix + `ref List<GeneDef> genes` | 通过 | 源码检查 |
| Prefix 最后优先级 | 通过 | `[HarmonyPriority(Priority.Last)]` |
| 阶段 1 GenerateGenes Postfix 保留 | 通过 | `GD_PawnGenerationPatch.cs` 未移除 |
| Newborn 跳过保留 | 通过 | `AllowedDevelopmentalStages.Newborn()` 与 `newborn request` 仍存在 |

## 3. 源列表隔离

| 检查项 | 结果 | 证据 |
|---|---|---|
| 未命中/失败时不替换原参数 | 通过 | `genes = mutatedGenes` 仅在 TryBuildMutatedCopy 成功后执行 |
| 不对源 genes Add/Remove/Clear | 通过 | Birth 源码检索无 `genes.Add/Remove/Clear` |
| 成功时复制列表 | 通过 | `new List<GeneDef>(sourceGenes) { candidate }` |
| 每次成功最多追加一个 | 通过 | 集合初始化器只加入一个 candidate，方法成功后立即 return |
| 每个候选位最多 20 次尝试 | 通过 | `MaxCandidateAttempts = 20` |

## 4. 概率与资格门

| 检查项 | 结果 | 证据 |
|---|---|---|
| 默认概率 10% | 通过 | `MutationChance = 0.10f` |
| 每次合格调用只投一次概率 | 通过 | `TryApply` 中仅一个 `Rand.Chance(MutationChance)`，候选循环不再投概率 |
| genes null 不消耗概率 | 通过 | IsEligible 在 RecordBirthChanceRoll 之前返回 |
| 遗传母亲必须原版 Human | 通过 | IsVanillaHuman(geneticMother) |
| 父亲非空时必须原版 Human | 通过 | father null 允许；非 null 调 IsVanillaHuman |
| Biotech inactive 安全跳过 | 通过 | ModsConfig.BiotechActive 防御门 |

## 5. 候选与安全校验

| 检查项 | 结果 | 证据 |
|---|---|---|
| 只从 MutationGenes 取阶段 2 候选 | 通过 | `snapshot?.MutationGenes` |
| selectionWeight 加权 | 通过 | 复用 `GD_GeneSelector.TrySelectWeighted` |
| 基础官方普通 Gene 过滤 | 通过 | 复用 `GD_WorldGenePool.IsVanillaPoolGene` |
| 重复/双向冲突/前置 | 通过 | `AcceptsForBirth` + `AcceptsAgainstExisting` |
| 覆盖后代谢 | 通过 | 列表重载构造 Endogene GeneDefWithType 后调用 NonOverriddenGenes |
| 代谢边界不用 internal Includes | 通过 | 直接比较 BiostatRange.min/max |
| 不伪造 Pawn | 通过 | 出生验证只接收 GeneDef 列表 |

## 6. 诊断

| 检查项 | 结果 | 证据 |
|---|---|---|
| 统计 patchCalls/nullGenes/chanceRolls/hits/misses | 通过 | `GD_Diagnostics.cs` Birth mutation diagnostics |
| 统计 mutationAdded/候选拒绝/耗尽 | 通过 | 独立计数与字典 |
| 正常模式不逐胎刷日志 | 通过 | Patch/utility 只累计计数 |
| 有只读查看入口 | 通过 | `只读查看第二阶段出生诊断` |
| 有列表隔离入口 | 通过 | `只读测试第二阶段列表隔离`，明确只消耗 RNG |

## 7. XML 与构建

| 检查项 | 结果 | 证据 |
|---|---|---|
| About.xml 可解析 | 通过 | PowerShell XML parse 成功 |
| PackageID | 通过 | `yyyyy.geneticdiversity` |
| supportedVersions | 通过 | `1.6` |
| Biotech/Harmony 依赖 | 通过 | `ludeon.rimworld.biotech`、`brrainz.harmony` |
| Release 构建 | 通过 | 2026-07-28 `Build succeeded` |
| 编译警告 | 通过 | 0 Warning(s) |
| 编译错误 | 通过 | 0 Error(s) |
| 最终开发/运行 DLL SHA-256 | 通过 | 8ADC84CD982C0B4BC7ACD4ADC48EDCFEC25C96878497B7388BB8D85FB09A0C0D |
| 运行副本白名单 | 通过 | 仅 About.xml 与 GeneticDiversity.dll |

## 8. 静态检查完成时尚未验证、现已由用户验收

以下项目必须在 RimWorld 中执行，不能由静态检查替代：

- ApplyBirthOutcome Patch 实际加载与一次调用次数；
- 自然怀孕真实非 null GeneSet；
- 阶段 1 旧档已有怀孕升级；
- HumanEmbryo 保存/读取/植入；
- 成长舱 `DEV: Embryo birth now`；
- 20/200 次概率统计；
- 多胎共享源列表；
- 带突变孩子保存读取；
- 移除模组回滚；
- 阶段 1 全回归；
- 与 Epona/其他出生 Patch 的游戏内共存。

后续结论：用户于 2026-07-28 提供列表隔离与 null genes 日志，并明确反馈其余全部通过。阶段 2 现已正式记录为**静态检查、Release 构建和游戏内验收全部通过**。详细记录见 `阶段2游戏内验收记录_2026-07-28.md`。
