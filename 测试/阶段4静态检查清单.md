# 基因多样性：阶段4静态检查清单

> 最后更新：2026-07-28  
> 范围：已知 HAR 基因来源精确兼容  
> 游戏内状态：用户日志已判定通过；阶段 4 测试专用源码已清理。清理后构建与同步待主会话完成。  
> 证据边界：当前日志是测试摘录而非完整 `Player.log`，不能宣称完整启动无红字。

## 1. 已完成静态检查

- [x] 新增公开 Def 类型 `GD_RaceGeneCompatibilityDef`，字段覆盖来源 packageId、Race、原生 Xenotype、原生/排除/同种族/跨种族安全 Gene、排除 PawnKind。
- [x] Def 名使用 `GetNamedSilentFail` 解析；缺失来源、Race、Xenotype 或 Gene 时不产生第三方静态类型引用。
- [x] 六个来源均使用 `MayRequire` 与运行时 packageId 检测。
- [x] csproj 没有引用 AlienRace、Kiiro、Milira、Wolfein、Ratkin、OA 或 Epona DLL。
- [x] 世界实际 Pawn Endogenes 的 4/2/1 权重逻辑未改；精确 Def 仅约束非法跨 Race 候选与无样本时的低权重回退。
- [x] 精确 Race 存在时不再读取 HAR 通用 Xenotype 列表作为回退，避免 Epona/Milira、基础/混血池混淆；HAR raceGenes 仍保留并继续经过 CanHaveGene。
- [x] Wolfein 只登记可遗传 `Wolfein_Xenotype`，排除 `Wolfein_PureBlood` 身份基因。
- [x] Ratkin Gene Expanded 只登记当前 1.6 的八个可遗传异种类型。
- [x] OA 只登记明确可遗传的七个 `OAGene_*` 普通鼠族异种类型（含 `OAGene_RatkinBase`）；自定义/结构类按来源包限制在 Ratkin，不登记不可遗传的 `OAGene_BiochemicalRatkinI`。
- [x] Epona、Unicorn、Destrier、Epona-Milira、Unicorn-Milira 分别建立独立 Race/Xenotype 规则。
- [x] Epona 出生逻辑未被 Patch；阶段2出生 Prefix 仍不设置 Xenotype。
- [x] 历史测试版曾新增 12 个准确中文阶段 4 Debug Action，其中 11 个单项/边界入口和 1 个阶段总入口；全部只读，验收记录完成后已随 `GD_Phase4AutomatedTests.cs` 删除。
- [x] About.xml 仅增加可选 `loadAfter`，没有新增硬依赖。
- [x] XML 文件可由 .NET XML 解析器读取。
- [x] `dotnet build --configuration Release`：0 warning、0 error。
- [x] 开发/运行 DLL SHA-256 一致：`88416386C00DF3BD2AA5AA9F67AF73E800D078240271041C7AEFB65C4621E0F0`。
- [x] 运行副本为最小白名单，仅含 About、DLL、兼容 Def 与简体中文 DefInjected。
- [x] 已刷新 Wiki，共 10 条兼容 Def，并补齐中文“游戏解释”列。

## 2. 构建结果

```text
GeneticDiversity -> 1.6/Assemblies/GeneticDiversity.dll
Build succeeded.
0 Warning(s)
0 Error(s)
```

## 3. 游戏内日志判定

- [x] 组合 A：Kiiro / Milira / Wolfein / Epona；阶段总汇总明确通过=8、失败=0、未执行=3。
- [x] 组合 B：Ratkin Gene Expanded 精确边界、结构边界和卸载软降级均通过。
- [x] 组合 C：OA Ratkin 精确边界、结构边界和自定义基因类边界均通过。
- [x] 三组合合并明确失败项为 0。
- [x] 结构基因与自定义类的组合 A 未执行项已由组合 B/C 补齐。
- [x] Epona-Milira 两个混血池通过。
- [x] 来源卸载软降级通过。
- [x] 绮罗族有直接实际值和最终结论，并由六来源总检查与阶段总汇总支持；因缺固定边界标记，记为带限定的通过。
- [ ] 完整启动无红字：现有摘录不是完整 `Player.log`，本项不能据此勾选。

## 4. 测试入口清理与待主会话事项

- [x] 原始日志已原样复制并核对 SHA-256。
- [x] 正式验收记录已创建。
- [x] `GD_Phase4AutomatedTests.cs` 已删除。
- [x] `GD_DebugActions.cs` 与六个永久诊断按钮保留。
- [ ] 清理后 Release 构建：**待主会话构建同步**。
- [ ] 清理后开发主副本 → 运行副本白名单同步：**待主会话构建同步**。
- [ ] 清理后 DLL SHA-256 一致性及最终程序集按钮名扫描：**待主会话构建同步**。

## 5. 证据边界

阶段 4 游戏内按钮日志已判定通过，但编译成功、旧测试版同步记录和测试摘录都不能替代清理后的构建同步，也不能证明完整启动无红字。当前正式结论为：**阶段 4 日志已判定通过、测试入口已清理；清理后构建与同步待主会话完成。**





