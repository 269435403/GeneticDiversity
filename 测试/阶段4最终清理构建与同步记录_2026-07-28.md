# 阶段 4 最终清理构建与同步记录

> 日期：2026-07-28  
> 范围：阶段 4 测试入口清理后的 Release 基线构建、最小运行副本同步与验证。  
> 边界：本次没有实现阶段 4.5，也没有修改阶段 2 三个红线源码文件。

## 1. 前置检查

- 开发主副本：`D:\steam\steamapps\common\RimWorld\Mods\临时存放\yyyyy_GeneticDiversity`
- 运行副本：`D:\steam\steamapps\common\RimWorld\Mods\yyyyy_GeneticDiversity`
- 两个绝对路径的末级目录均为 `yyyyy_GeneticDiversity`。
- 开发主副本父目录为 `Mods\临时存放`；运行副本父目录为 `Mods`，且运行目标不是 `Mods` 根目录。
- 同步前检查 `RimWorldWin64`：未运行。
- 已读取 `Source/GeneticDiversity/GeneticDiversity.csproj` 与根目录 `build.ps1`；未发现 `Directory.Build.props` 或 `.example` 配置。
- `dotnet --info`：SDK `10.0.301`，MSBuild `18.6.4`。
- 引用验证：
  - `D:\steam\steamapps\common\RimWorld\RimWorldWin64_Data\Managed\Assembly-CSharp.dll`：存在。
  - `D:\steam\steamapps\common\RimWorld\RimWorldWin64_Data\Managed\UnityEngine.CoreModule.dll`：存在。
  - `D:\steam\steamapps\workshop\content\294100\2009463077\Current\Assemblies\0Harmony.dll`：存在，并由 `build.ps1` 明确传入 `HarmonyPath`。

## 2. Release 构建

执行项目标准入口：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\steam\steamapps\common\RimWorld\Mods\临时存放\yyyyy_GeneticDiversity\build.ps1" -Configuration Release -RimWorldDir "D:\steam\steamapps\common\RimWorld"
```

结果：

- Build succeeded。
- 0 Warning(s)。
- 0 Error(s)。
- 输出：`D:\steam\steamapps\common\RimWorld\Mods\临时存放\yyyyy_GeneticDiversity\1.6\Assemblies\GeneticDiversity.dll`。
- 最终 DLL SHA-256：`88418CE0633359AEE7F585DB901D0237E4C43D776FF0062A96349DFB2793B6E0`。

## 3. 白名单同步

最终运行白名单严格为：

1. `About/About.xml`
2. `1.6/Assemblies/GeneticDiversity.dll`
3. `1.6/Defs/Compatibility/GD_KnownHarGeneSources.xml`
4. `1.6/Defs/Compatibility/GD_EponaRacePools.xml`
5. `1.6/Languages/ChineseSimplified/DefInjected/GeneticDiversity.GD_RaceGeneCompatibilityDef/GD_Compatibility.xml`

同步时先检查运行副本内的重解析点，再删除非白名单文件、清理空目录并复制五个白名单文件。本次运行副本原有非白名单文件数量为 0；同步后递归核对恰好 5 个文件，没有 Source、测试、文档、Wiki、项目文件、构建脚本或中间产物，也没有影响其他模组目录。

## 4. SHA-256 一致性

五个白名单文件的开发主副本与运行副本 SHA-256 全部一致。逐文件结果见 `测试/阶段4最终清理文件SHA256_2026-07-28.md`。

## 5. 测试入口字符串扫描

扫描范围：开发主副本 `Source/GeneticDiversity/*.cs` 与最终 `1.6/Assemblies/GeneticDiversity.dll`（按 UTF-8/UTF-16 字符串检查）。

下列 12 个阶段 4 临时按钮名在源码与 DLL 中均不存在：

1. `一键检查六个精确兼容来源是否检测`
2. `一键测试阶段4绮罗族精确边界`
3. `一键测试阶段4米莉拉精确边界`
4. `一键测试阶段4沃芬精确边界`
5. `一键测试阶段4鼠族基因扩展精确边界`
6. `一键测试阶段4OA鼠族精确边界`
7. `一键测试阶段4埃波娜精确边界`
8. `一键检查阶段4结构基因不会跨种族`
9. `一键检查阶段4自定义基因类不会跨种族`
10. `一键检查阶段4埃波娜米莉拉池不混淆`
11. `一键检查阶段4来源卸载软降级`
12. `一键执行阶段4全部只读测试`

`GD_Phase4AutomatedTests` 在源码与 DLL 中也均不存在。

下列六个永久按钮在源码与 DLL 中均存在：

1. `清空基因池缓存`
2. `立即刷新基因池缓存`
3. `只读统计当前地图成年人物`
4. `只读查看第三阶段HAR适配器报告`
5. `只读查看世界系谱池按种族摘要`
6. `只读查看第二阶段出生诊断`

## 6. 阶段 4.5 前红线基线

阶段 2 三个红线文件本次未修改；SHA-256 与既有阶段 3 清理后基线一致，已记录到 `测试/阶段4最终清理文件SHA256_2026-07-28.md`，供阶段 4.5 开始前复核。

## 7. 最终结论

阶段 4 测试入口清理后的 Release 基线构建、五文件白名单同步、开发/运行 SHA-256 一致性与按钮字符串扫描全部通过。阶段 4.5 未开始，创作之书未更新。
