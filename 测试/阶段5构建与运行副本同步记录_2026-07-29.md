# 阶段 5 构建与运行副本同步记录（2026-07-29）

## 1. 实现摘要

- 新增 `Source/GeneticDiversity/GD_FrdAdapter.cs`。
- 新增临时测试源码 `Source/GeneticDiversity/GD_Phase5AutomatedTests.cs`。
- 修改 `GD_WorldGenePool.cs`：目标 Race 无世界样本时加入 FRD 配置基因先验；FRD 可用时即使 HAR 不可用，Human 仍进入目标池逻辑。
- 修改 `GD_PawnGenerationPatch.cs`：HarmonyAfter 加入 `yyyyy.factionracediversity`。
- 修改 `GD_HarmonyBootstrap.cs`、`About/About.xml` 与项目文档；FCD 保持独立。
- 未修改 Mixed Peoples Factions 或 Faction Cultural Diversity 项目；现有公开 API 足够，无需给兄弟项目增加 Integration API。

## 2. 验证命令

- 严格配置校验：退出码 0，errors=0，warnings=0。
- XML 解析：5 文件，错误 0。
- Wiki mixed 生成：退出码 0；22 Def、22 中文名、44 DefInjected 匹配、0 未匹配、0 重复、0 空值、0 冲突。
- Release 构建：退出码 0，0 warning，0 error。
- `sync --apply --prune`：退出码 0；最终仅覆盖 DLL，运行污染扫描通过。

## 3. 运行副本

运行目录：`D:\steam\steamapps\common\RimWorld\Mods\yyyyy_GeneticDiversity`

- 文件数：7。
- 白名单：`About` + `1.6`；无 Source、Wiki、测试、项目文件或文档。
- 开发/运行 `About.xml` SHA-256 均为 `E4698B420156B84F0A3E69BF32573B4B4750A1CCEC36C349B27F9883D226607A`。
- 开发/运行 DLL SHA-256 均为 `5DF0A81101A1FA0EDF8E5667956E62F23E78CF65BFB747E6C89BEF7C3F6BCDEB`。
- 其余 5 个 XML/翻译白名单文件也逐文件一致。

## 4. 当前交付边界

这是带阶段 5 临时测试按钮的游戏内待验收版。静态检查、构建、Wiki 与同步通过不等于游戏内验收通过；收到并判定必要组合日志前保留 `GD_Phase5AutomatedTests.cs`，不得进入阶段 6。

