# 基因多样性：阶段 3 静态检查清单

> 日期：2026-07-28  
> 说明：本清单最初记录 Agent 的静态/构建验证；2026-07-28 收到用户日志后，游戏内项目已补充判定并完成测试按钮清理。正式结论见 `阶段3游戏内验收记录_2026-07-28.md`。

## A. 权威接口核对

- [x] HAR `RaceRestrictionSettings.CanHaveGene(GeneDef, ThingDef, bool)` 签名与 bool 返回值已从本地 HAR 源码核对。
- [x] HAR `RaceRestrictionSettings.CanUseXenotype(XenotypeDef, ThingDef)` 签名与 bool 返回值已核对。
- [x] `ThingDef_AlienRace.alienRace.generalSettings.raceGenes` 字段路径已核对。
- [x] `AlienChanceEntry<T>` 的 `entry/options/count/chance/shuffledOptions/Select` 行为已核对。
- [x] HAR `GenerateGenesForcedHelper` 在原版 GenerateGenes 内加入 raceGenes 的行为已核对。
- [x] HAR `Pawn_GeneTracker.AddGene` Prefix 返回 null 并跳过原方法的最终裁决已核对。

## B. 软兼容

- [x] csproj 不引用 AlienRace.dll。
- [x] 源码无 `using AlienRace` 或 HAR 静态类型引用。
- [x] Release DLL 引用列表仅含 mscorlib、Assembly-CSharp、0Harmony、System.Core、System；无 AlienRace 程序集。
- [x] About.xml 未把 HAR 加入 modDependencies。
- [x] About.xml 仅在 loadAfter 加入 `erdelf.HumanoidAlienRaces`。
- [x] HAR 未安装时适配器返回 not detected，Human 路径保留，非 Human 保守跳过。
- [x] 适配失败只警告一次并熔断。

## C. 反射与缓存

- [x] 只在初始化时扫描已加载程序集和核对 MethodInfo。
- [x] MethodInfo 转成基类参数委托，候选热路径不重复 Invoke/查签名。
- [x] CanHaveGene 与 CanUseXenotype 结果按 Race 缓存。
- [x] HAR Race 类型判定、明确 Xenotype、可能 raceGenes 按 Race 缓存。
- [x] 缓存清空动作会清除世界快照与 HAR Race/判定缓存。
- [x] 正常模式无逐 Pawn 反射警告日志。
- [x] “一键测试系谱基因四比二比一权重”会自动构造内存样本、验证 40/20/10 并输出准确中文结果，无需手工生成或统计 Pawn。

## D. raceGenes/Xenotype 回退

- [x] 未调用 AlienChanceEntry.Select 或 Approved。
- [x] chance ≤ 0 静态排除。
- [x] direct entry 不受 count 影响；count ≤ 0 不递归 options。
- [x] 读取 xenotypeList、whiteXenotypeList、blackXenotypeList。
- [x] Xenotype 回退再次通过 CanUseXenotype。
- [x] Gene 回退再次通过 CanHaveGene(gene, race, false)。
- [x] Def 回退权重 0.25，低于任何一次当前世界观察的基础权重 1。

## E. 世界系谱池

- [x] 扫描前对 AllMapsWorldAndTemporary_Alive 调用 ToList。
- [x] HashSet 去重 Pawn。
- [x] 只统计 Humanlike、有 gene tracker 的 Human/HAR Pawn。
- [x] 只统计 Endogenes。
- [x] 保存 Gene、Race、Faction、次数和候选类别。
- [x] 同阵营同 Race ×4、同 Race ×2、跨 Race 标准候选 ×1。
- [x] 自定义 geneClass 和结构/外观候选不跨 Race。
- [x] 快照每 60,000 ticks 刷新。
- [x] Race/Faction 目标池在快照内缓存。
- [x] 原版 Human 常见池与官方突变池单独保留，阶段 2 候选语义不被 HAR 观察数据替换。

## F. 安全与 Patch

- [x] GenerateGenes 使用显式三参数签名。
- [x] 标注 HarmonyAfter `rimworld.erdelf.alien_race.main`。
- [x] Newborn 与强制自定义/强制基因跳过条件保留。
- [x] 非 Human 必须 Humanlike、HAR 可用、HAR Race。
- [x] 标准/自定义/结构候选分层。
- [x] 重复、双向冲突、前置、覆盖后代谢、Violent 检查保留。
- [x] AddGene null 后换候选，每位点最多 20 个不同候选。
- [x] 阶段 3 不扩展 HAR 出生突变。

## G. 阶段 2 回归哈希

- [x] `GD_BirthMutationPatch.cs` SHA-256 与阶段 2 基线一致。
- [x] `GD_BirthMutationUtility.cs` SHA-256 与阶段 2 基线一致。
- [x] `GD_MetabolismUtility.cs` SHA-256 与阶段 2 基线一致。

## H. 构建与元数据

- [x] About.xml 可作为 XML 解析。
- [x] PackageID 为 `yyyyy.geneticdiversity`。
- [x] Harmony ID 为 `yyyyy.geneticdiversity`。
- [x] supportedVersions 含 1.6。
- [x] Release 构建 0 warning、0 error。
- [x] 阶段 3 文档、白话测试、静态清单、游戏内记录已创建/更新。
- [x] 实际 RimWorld HAR 未安装回归：日志显示 `harAware=False`，原版 Human 路径正常，无适配器失败。
- [x] 实际 RimWorld HAR 通用矩阵：HAR 适配器 ready，HAR 人物进入生成路径，四比二比一和多项 Race 安全边界通过。
- [x] 缓存与读取性能：刷新扫描 162 人/2 种族耗时 10 毫秒，重复读取 1000 次耗时 2 毫秒，无异常；100/500/1000 人压力矩阵留作后续大改回归，不作为阶段 3 阻塞项。
- [x] 保存读取与移除安全结构：四项数据重载后不变，自定义持久类型数量为 0。

## I. 双副本交付

- [x] Release DLL 输出到开发主副本。
- [x] 首次同步检测到 RimWorldWin64 正在运行并安全等待，未终止用户游戏；进程正常退出后执行复制。
- [x] About.xml 与 DLL 白名单同步到根目录运行副本。
- [x] 运行副本无 Source、模组文档、测试、Wiki、build.ps1、csproj、bin/obj。
- [x] 开发/运行 DLL SHA-256 一致。

最终 DLL SHA-256 与同步时间见 `阶段3构建与运行副本同步记录_2026-07-28.md`。



## J. 测试按钮自动化与中文

- [x] 新增 `GD_Phase3AutomatedTests.cs`。
- [x] 第三阶段测试方案中的每个普通玩家测试都有对应开发者操作按钮。
- [x] 按钮名称与测试文档逐字一致。
- [x] 四比二比一权重由按钮自动构造 40/20/10，不要求人工统计。
- [x] HAR 临时人物由按钮自动生成并立即丢弃。
- [x] HAR 基因限制、种族固有基因随机安全、异种类型限制和特殊基因边界均可一键只读检查。
- [x] 缓存测试自动执行一千次读取并报告耗时。
- [x] 阶段一概率测试自动执行十万次固定随机样本。
- [x] 保存读取使用准备/验证/清理三个按钮，不新增游戏存档组件。
- [x] 移除测试改为一键扫描自定义持久类型；普通玩家不再被要求冒险禁用模组加载正式存档。
- [x] 测试方案删除人物编辑器、手工多组样本、批量数百人物和人工计算要求。
- [x] 玩家可见测试按钮与主要结果使用准确中文。

## K. 验收后测试按钮清理

- [x] 用户原始日志已复制到项目测试目录。
- [x] Agent 已完成全部测试表格与通过/证据边界判定。
- [x] `GD_Phase3AutomatedTests.cs` 已删除。
- [x] `一键测试系谱基因四比二比一权重` 已删除。
- [x] `只读测试第二阶段列表隔离` 已删除。
- [x] 最终只保留六个长期缓存/只读诊断按钮。
- [x] 删除后 Release 构建 0 warning、0 error。
- [x] 删除后开发/运行 DLL SHA-256 一致。
