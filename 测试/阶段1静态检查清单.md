# 基因多样性：阶段 1 静态检查清单

> 最后更新：2026-07-28  
> `[x]` 表示已由当前真实实现、主会话构建结果或用户游戏内验收反馈确认。阶段 1 于 2026-07-28 由用户确认全部通过、无错误；Agent 未在本会话亲自操作游戏。

## A. 元数据与范围

- [x] PackageID 为 `yyyyy.geneticdiversity`。
- [x] Harmony ID 为 `yyyyy.geneticdiversity`。
- [x] supportedVersions 为 1.6。
- [x] About 硬依赖 Biotech 与 Harmony。
- [x] 不安排无 Biotech 加载测试。
- [x] 阶段 1 无出生突变、HAR、旧档回填、设置 UI、功能 GeneDef。

## B. Harmony 与请求资格

- [x] Patch 目标为 `PawnGenerator.GenerateGenes` Postfix。
- [x] HarmonyPatch 显式指定 `(Pawn, XenotypeDef, PawnGenerationRequest)` 参数签名。
- [x] 显式标注 `[HarmonyPostfix]`。
- [x] Newborn 请求跳过。
- [x] ForcedCustomXenotype 非空时跳过。
- [x] ForcedEndogenes 使用 `NullOrEmpty()` 判断。
- [x] ForcedXenogenes 使用 `NullOrEmpty()` 判断。
- [x] 空 Forced 列表不会被 `!= null` 误判。
- [x] 只有 ForcedXenotype 时不误跳过，也不改变 Xenotype。
- [x] 只处理原版 Human、Humanlike、有 gene tracker 的 Pawn。

## C. 世界基因池

- [x] `AllMapsWorldAndTemporary_Alive` 立即 `ToList()` 后再枚举。
- [x] 使用 HashSet 防止同一 Pawn 重复统计。
- [x] 常见池只统计 Human Endogenes。
- [x] Xenogenes 不进入常见遗传池。
- [x] 空世界池回退到官方可遗传 XenotypeDef。
- [x] 缓存存在自动过期、清空和立即刷新入口。
- [x] Mutation pool 排除 common pool 已出现基因。
- [x] 突变按 selectionWeight 加权。

## D. 候选基础过滤

- [x] 只接受官方 Def。
- [x] `geneClass == typeof(Gene)`。
- [x] 排除 `bodyType.HasValue`。
- [x] 排除非空 `forcedHeadTypes`。
- [x] 排除 HairColor 与 Melanin。
- [x] 排除 `GeneDefOf.Inbred`。
- [x] 排除 `biostatArc > 0`。
- [x] 要求 `canGenerateInGeneSet`。
- [x] Mutation pool 要求 selectionWeight > 0。

## E. 0～3 与 90/10

- [x] 0/1/2/3 位点概率为 20%/55%/20%/5%。
- [x] 90/10 对每个位点独立选择来源。
- [x] 每个位点最多尝试 20 次。
- [x] 同一位点不重复尝试同一候选。
- [x] AddGene 返回空后记录并避免再次选择该候选。
- [x] 候选不足时允许空缺，无死循环。
- [x] 最终使用 `xenogene: false` 加入。

## F. 冲突、前置、代谢与暴力

- [x] 检查当前 Endogenes/Xenogenes 重复。
- [x] 双向检查 `candidate.ConflictsWith(existing)` 和反向冲突。
- [x] prerequisite 必须已经存在，不自动补链。
- [x] 存在独立 `GD_MetabolismUtility.cs`。
- [x] 当前 Xenogenes 以 xenogene 类型加入预模拟。
- [x] 当前 Endogenes 与候选以 endogene 类型加入预模拟。
- [x] 使用 `GeneDefWithType.NonOverriddenGenes()`。
- [x] 代谢只累计未被覆盖的最终有效基因。
- [x] Violent 禁用也检查最终未覆盖有效基因。
- [x] `MustBeCapableOfViolence` 请求会拒绝最终禁用 Violent 的候选。
- [x] 代谢范围比较 `GeneTuning.BiostatRange.min/max`。
- [x] 未再调用不可访问的 `IntRange.Includes`。

## G. Debug Actions

- [x] “清空基因池缓存”存在，只清缓存。
- [x] “立即刷新基因池缓存”存在，输出候选数量。
- [x] “只读统计当前地图成人 Human”存在，并立即 ToList 复制地图 Pawn 集合。
- [x] 只读统计输出 xenotype、Endogene 分布、池摘要和诊断。
- [x] 没有声称原版 Debug Actions 能直接构造 ForcedCustomXenotype 请求。
- [x] ForcedCustom/Forced gene 精确游戏内测试标为可选高级测试。

## H. 保存与阶段边界

- [x] 阶段 1 无自定义存档字段。
- [x] 变体由原版 gene tracker 保存为 Endogenes。
- [x] 没有 ApplyBirthOutcome Patch。
- [x] 新生儿资格判断明确跳过。
- [x] 没有旧档批量回填逻辑。

## I. 构建

- [x] TargetFramework 为 net48。
- [x] Release 输出到开发主副本 `1.6/Assemblies/`。
- [x] Assembly-CSharp、Unity、Harmony 引用 Private=False。
- [x] 首次构建的 `IntRange.Includes` internal 问题已修正。
- [x] 最终 Release 构建 0 警告、0 错误。
- [x] `GeneticDiversity.dll` 存在且非空。

## J. 文档与测试

- [x] 四件套齐全。
- [x] 修改日志顶部有 2026-07-27 整合修正与重构建条目。
- [x] 逐文件说明登记 `GD_MetabolismUtility.cs`。
- [x] 测试方案明确 Biotech 为硬依赖。
- [x] 测试方案无“禁用 Biotech 仍加载”场景。
- [x] 测试方案包含加载、开发者模式、普通/战斗 Pawn、Race/Xenotype/Endogenes、保存读取和遗传。
- [x] 关键步骤有前值、后值、通过、失败和状态影响。
- [x] 不稳定入口/统计标为可选高级测试，不要求手改存档。
- [x] 文档明确构建通过不等于游戏内通过。

## K. 运行副本与游戏内状态

- [x] 当前开发主副本 DLL 已生成。
- [x] 根目录 `Mods/yyyyy_GeneticDiversity/` 运行副本已创建并完成白名单同步。
- [x] RimWorld 最小加载无红字。
- [x] 本模组缓存/只读统计 Debug Actions 游戏内通过。
- [x] 普通新 Pawn 游戏内抽样通过。
- [x] 战斗/Violent 游戏内通过。
- [x] 保存读取通过。
- [x] 原版遗传和新生儿跳过通过。
- [x] 旧人物不回填通过。
- [x] 性能与日志检查通过，无本模组红字、错误或明显性能问题。

## 当前结论

阶段 1 静态实现、Release 构建、运行副本同步和游戏内验收均已通过。用户于 2026-07-28 明确反馈“测试全部通过，无错误”。游戏内结论来源于用户反馈，Agent 未在本会话亲自操作 RimWorld。

