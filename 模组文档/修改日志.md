# 修改日志

> 最后更新：2026-07-30｜最新条目置顶。PackageID：yyyyy.geneticdiversity。

## 2026-07-30｜基因数量显示优化与黑名单功能

### 功能新增

1. **基因数量概率显示**：在 ModSettings 强度设置下方直接显示当前档位的实际概率分布
   - 保守：0个45%，1个45%，2个14%，3个1%
   - 标准：0个20%，1个55%，2个20%，3个5%（默认）
   - 高：0个5%，1个35%，2个40%，3个20%
   - 让玩家清楚了解调整强度对基因数量的具体影响

2. **基因黑名单系统**：新增两种黑名单机制，允许玩家排除不想要的基因
   - 按基因 defName 黑名单：可以精确禁用单个基因
   - 按基因类别 (GeneCategoryDef) 黑名单：可以禁用整个类别的基因
   - 提供"管理黑名单基因"和"管理黑名单类别"按钮，弹出可搜索的浮动菜单
   - 黑名单基因在所有随机生成（新人物生成、出生突变）中都会被过滤
   - 黑名单会持久化保存在 ModSettings 中

### 技术实现

- `GD_Settings.cs`：新增 `BlacklistedGenes` 和 `BlacklistedGeneCategories` 字段，新增 `IntensityDistribution` 方法
- `GD_GeneSafetyValidator.cs`：在 `Accepts` 和 `AcceptsForBirth` 中增加黑名单检查
- `GD_SettingsWindow.cs`：增加滚动视图支持更多设置项，实现黑名单管理UI

### 构建与同步

- Release 构建：0 警告、0 错误
- 按 `project.yaml` 完成 sync 预览、`sync --apply` 与 `sync-verify`
- 运行副本 7 文件一致，污染 0，未使用 `--prune`
- 修改版开发/运行 DLL：75776 字节，SHA-256 `6D47232B0AE8D8EEBA403D3196259C437089FF147027695451064A76852E0A36`

### 用户测试说明

需要在游戏内测试以下功能：
1. 打开 ModSettings 确认强度设置下方显示概率分布
2. 点击"管理黑名单基因"，选择几个基因加入黑名单
3. 点击"管理黑名单类别"，选择一个类别加入黑名单
4. 使用 Dev Mode 生成新人物，确认黑名单基因不会出现
5. 退出游戏重新加载，确认黑名单设置已保存

## 2026-07-30｜阶段 5 游戏内全通过与最终收口

- 用户确认随机基因修复回归全部通过；新 `Player.log` 同轮加载 GD、FRD、FCD，工作台 `accept` 为 PASS，`related_errors=0`。
- 回归按钮生成 20 个临时成年 Colonist，清理 20；GenerateGenes 调用和合格人物增量均为 20；随机位点增量 24，成功新增基因 24，异常 0，最终结论通过。
- 本批主世界系谱/FRD 主池已经能够提供合法候选，所以恢复池尝试/成功为 0；受限恢复池继续作为主池候选全部不兼容时的防御性保底路径。
- 删除 `GD_Phase5AutomatedTests.cs`；严格校验 0 error/0 warning，Release 构建 0 warning/0 error。
- sync 预览仅复制 DLL，`prune=0`；完成无 prune `sync --apply` 和 `sync-verify`，开发/运行 7 文件一致，运行污染 0。
- 最终源码与 DLL 均不含阶段 5 临时类和按钮字符串，只保留六个长期诊断 Debug Action。
- 最终开发/运行 DLL 均为 75776 字节，SHA-256 `83FA51F6F9B9575F863B1D423236EA980533D857CC90CEA7BD66288FCFDA82A3`。
- 阶段 5 已完整验收并收口；阶段 6 尚未开始。
## 2026-07-30｜阶段 5 联动验收与随机基因恢复修复

### 日志验收

- 保存用户本轮完整 `Player.log` 副本并执行工作台 `accept`；同轮实际加载 GD、FRD、FCD，相关错误 0。
- 阶段 5 原五项联动测试全部通过：FRD 状态、先验/世界样本优先、不重选 Race/PawnKind/Xenotype、三模组联合内存测试、软降级契约；总按钮汇总为通过 5、失败 0、未执行 0。
- 原测试只验证只读契约，没有生成真实 Pawn，因此不能证明随机基因生成链路。

### 回归原因与修复

- 定位到 Common 位点的恢复缺口：世界系谱/FRD 主池非空，但对新 Pawn 而言全部候选重复、冲突、缺前置、代谢不合法或 Race 不兼容时，旧逻辑在 20 次候选耗尽后直接放弃位点。
- `GD_WorldGenePool.cs` 新增按目标 Race 构造的受限恢复池；Human 使用原版可遗传 Xenotype 的标准基因低权重池，HAR 使用 raceGenes、精确兼容 Def 与允许 Xenotype，FRD 正权重先验仍可参与。
- `GD_GeneSelector.cs` 保持世界 4/2/1 与 FRD 主池优先，只有主池无法加入合法基因时才尝试恢复池；10% Mutation 路径不变。恢复候选仍必须通过精确兼容、HAR `CanHaveGene`、重复、冲突、前置、代谢、暴力能力与 `AddGene` 最终裁决。
- `GD_Diagnostics.cs` 新增恢复池尝试/成功计数；`GD_Phase5AutomatedTests.cs` 新增 `一键生成阶段5随机基因修复回归样本`，自动临时生成并清理 20 个成年 Colonist，验证正式生成链路确实产生随机位点并成功新增基因。

### 构建与交付

- 严格校验：0 error、0 warning；Release 构建：0 warning、0 error。
- 按 `project.yaml` 完成 sync 预览、`sync --apply` 与 `sync-verify`，未使用 `--prune`；运行副本 7 文件一致、污染 0。
- 修复版开发/运行 DLL：87552 字节，SHA-256 `EC7C2E1749B6F799AB389429566944E75965D68B4F97A8696D266520CCD8A24B`。
- 当前仍保留阶段 5 测试源码。用户必须手动重启 RimWorld 后运行新回归按钮并提供新 `Player.log`；通过前不删除测试入口、不最终收口阶段 5，也不开始阶段 6。

## 2026-07-29｜阶段 4.5 收口与阶段 5 FRD/FCD 联动测试版

- 用户明确确认阶段 4.5 全部测试通过；删除 `GD_Phase45AutomatedTests.cs`，Release 重建 0 警告/0 错误并按 `project.yaml` 白名单同步运行副本。
- 新增 `GD_FrdAdapter.cs`，通过可选反射读取 FRD 的公开种族登记、人物种类、异种类型安全服务、`raceWeights` 与按种族 Xenotype 权重；未增加第三方程序集硬引用。
- 当目标 Race 没有当前世界 Endogene 样本时，把 FRD 正权重 Xenotype 配置转换为总权重 0.5 的安全基因先验；现有世界 4/2/1 观察仍严格优先。
- `PawnGenerator.GenerateGenes` 的 Harmony 顺序声明在 HAR 与 FRD 之后；适配器不写 `PawnGenerationRequest`，不重选 Race、PawnKind、Xenotype 或政治阵营。
- FCD 只做三模组联合独立性回归，不建立文化与基因的自动绑定。
- 新增 `GD_Phase5AutomatedTests.cs` 与五个单项/一个阶段总按钮；当前是带临时测试入口的游戏内待验收版本。测试版 DLL SHA-256：`5DF0A81101A1FA0EDF8E5667956E62F23E78CF65BFB747E6C89BEF7C3F6BCDEB`。
## 2026-07-29｜纯工作区迁移与工程路径边界

- 当前开发主副本路径迁移为 `D:\RimWorldModding\projects\yyyyy_GeneticDiversity\`；本条只记录工作区与工程路径迁移，不改变任何玩法、Def、兼容规则、测试结论或运行副本内容，也未同步或写入运行副本。
- 同批 A 类工程修改：阶段 2 与阶段 3 同步脚本均将开发源目录与运行目标目录改为明确绝对路径，并以规范化路径、目录分隔边界、禁止根目录、末级同名及精确白名单收紧安全边界；`ValidateOnly` 仅验证路径、白名单与计划内容，不查询或等待游戏，不进行构建、同步、日志写入或其他写入；`GeneticDiversity.csproj` 的 `RimWorldDir` 改为可外部传入并提供本机默认值，HAR/Harmony 工作台 fallback 随新工作区迁移，不再依赖旧开发目录层级。
- `模组逐文件白话说明.md` 未更新：本次没有文件增删、重命名、运行结构变化或玩家可改性变化；工程脚本和项目路径调整属于内部开发流程。

## 2026-07-28｜阶段 4.5 十来源精确兼容测试版

### 实现

- 新增 GD_Phase45HarGeneSources.xml 与对应简体中文 DefInjected，补齐 Nivarian、Ratkin、OA、四个 Kiiro 猫种、Dragonian、Axolotl、Yuran 共十个实际加载来源；About.xml 仅增加可选 loadAfter，没有新增硬依赖。
- GD_CompatibilityRegistry 扩展精确所有者边界；测试同时覆盖显式 sameRaceOnlyGenes 与自动分类为非标准的原生基因，避免 Nivarian 自定义 geneClass 因 XML 漏列跨 Race。
- Moosesian 未登记：其 Biotech XML 不在默认活动 Defs/ 根，且模组没有 LoadFolders.xml，RimWorld 1.6 当前不会加载这些 Def。

### 测试与工程

- 新增 GD_Phase45AutomatedTests.cs 与 阶段4.5白话测试方案.md；主要入口为 一键执行阶段4.5当前组合全部只读测试。世界优先级测试使用纯内存 Race/Faction 与正式 GD_WorldGenePool.GetCommonGenesFor，断言 4/2/1 且不写 DefDatabase、缓存或存档。
- 静态检查：运行 XML 解析失败 0、项目 DefName 重复 0、11 条阶段 4.5 规则、22 个翻译键缺失 0、十来源 packageId/Def 缺失 0、总失败数 0。
- Release 构建 0 warning、0 error；Def Wiki 已刷新为 21 条并补齐人工“游戏解释”；按七文件测试版白名单同步开发主副本到运行副本并逐文件核对 SHA-256。测试版 DLL SHA-256 为 `7662A9A01FE9DA889D82B118B6A540D90C78BA08499E347923B506CFEBB9EF1B`。
- 新增 `测试/阶段4.5Release构建输出_2026-07-28.log`、`阶段4.5构建与运行副本同步记录_2026-07-28.md` 与 `阶段4.5测试版文件SHA256_2026-07-28.md`。
- 阶段 2 三个红线文件哈希保持不变。

### 当前边界

- 当前是带 GD_Phase45AutomatedTests.cs 的游戏内待验收测试版。未收到最终覆盖十个 packageId 的用户日志前，不得删除测试源码，不得宣称阶段 4.5 游戏内通过。
- 阶段 4 已完整收尾；阶段 5、阶段 6 未开始。
## 2026-07-28｜阶段 4 游戏内验收闭环与测试入口清理

### 证据与判定

- 将用户日志从 `模组制作工作台/其他文档/日志.txt` 原样复制为 `测试/阶段4用户测试原始日志_2026-07-28.txt`；来源与副本 SHA-256 均为 `27EF95012AF94ED9FA2683351EB88C543EE3158E817097FD52795C9EB4F0BDFF`，原日志未修改。
- 新增 `测试/阶段4游戏内验收记录_2026-07-28.md`，由 Agent 按日志自动判定；三组合合并明确失败为 0，阶段 4 通过。
- 绮罗族单项缺固定开始/结束边界标记，但有完整直接值、失败原因和最终结论，并由六来源总检查与阶段总汇总交叉支持，因此记为带限定的通过。
- 现有材料是测试日志摘录，不是完整 `Player.log`；本次没有宣称完整启动无红字。

### 测试入口清理

- 删除 `Source/GeneticDiversity/GD_Phase4AutomatedTests.cs` 及其中 12 个阶段 4 临时 Debug Action。
- 保留 `GD_DebugActions.cs` 和六个永久诊断按钮。
- 统一阶段 4 文档术语为 OA / `OAGene_*`，修正旧简称与旧 Def 前缀漂移。
- 清理后已使用标准 `build.ps1` 完成 Release 基线构建：0 warning、0 error；五文件白名单已同步，开发/运行副本逐文件 SHA-256 全部一致。
- 清理后最终 DLL SHA-256：`88418CE0633359AEE7F585DB901D0237E4C43D776FF0062A96349DFB2793B6E0`；源码与最终 DLL 均无 12 个阶段 4 临时按钮名及 `GD_Phase4AutomatedTests`，六个永久诊断按钮仍存在。
- 新增 `测试/阶段4最终清理构建与同步记录_2026-07-28.md` 与 `测试/阶段4最终清理文件SHA256_2026-07-28.md`；阶段 2 三个红线文件未修改，其哈希已记录为阶段 4.5 前基线。本次未实现阶段 4.5。

## 2026-07-28｜阶段 4 已知 HAR 基因来源精确兼容

### 核心实现

- 新增 `GD_RaceGeneCompatibilityDef` 与 `GD_CompatibilityRegistry`，用本模组 Def 精确描述来源 packageId、Race、原生 Xenotype/Gene 与传播边界。
- 世界实际 Endogene 的 4/2/1 权重保持不变；精确规则只用于低权重 Def 回退和非法跨 Race 候选拦截。
- 精确 Race 存在时不再把 HAR 通用 Xenotype 列表全部当回退，避免基础 Milira、基础 Epona 和两个混血池混淆；HAR raceGenes 与 CanHaveGene 最终裁决仍保留。
- Kiiro、Milira、Wolfein、Ratkin Gene Expanded、OA Ratkin、Epona 五个 Race 分池组均已建立 XML 规则。
- Wolfein 纯血身份基因排除；OA 未显式可遗传的 `OAGene_*` 异种类型不转成系谱回退；结构和自定义 geneClass 默认只在声明 Race 内。

### 测试与文档

- 新增 `GD_Phase4AutomatedTests.cs`，提供 12 个准确中文只读 Debug Action，包括阶段总按钮。
- 新增《阶段4白话测试方案》和《阶段4静态检查清单》；明确 Ratkin Gene Expanded 与 OA Ratkin 需要分开加载测试。
- 新增 10 条兼容 Def Wiki，补齐简体中文与人工“游戏解释”列。
- 更新 About.xml 可选 loadAfter、模组总文档、逐文件说明、修改注意事项与核心设计公约。

### 构建状态

- 修正 csproj 默认 `RimWorldDir` 相对层级，使直接 `dotnet build` 与 build.ps1 都能正确定位游戏程序集。
- Release 构建：0 warning、0 error。
- 历史测试版状态：当时待用户执行三个加载组合并提供日志，测试按钮尚未删除；后续已由本文件顶部“阶段 4 游戏内验收闭环与测试入口清理”条目完成判定和源码清理。

## 2026-07-28｜阶段 3 HAR 通用兼容实现

### 核心实现

- 新增 `GD_HarAdapter.cs`：无 AlienRace.dll 硬依赖，一次性核对 HAR 1.6 类型/签名并创建基类参数委托；CanHaveGene、CanUseXenotype、Race 类型、raceGenes 与显式 Xenotype 均按 Race 缓存。
- `GD_WorldGenePool.cs` 扩展为 Human/HAR Race/Faction 系谱快照，保存当前世界 Endogene 来源并实现同阵营同 Race ×4、同 Race ×2、跨 Race 标准候选 ×1。
- 目标 Race 无样本时低权重读取 HAR raceGenes 与允许 Xenotype；只读取 AlienChanceEntry 的 entry/options/count/chance，不调用 Select/Approved。
- 标准 Gene 可在 HAR 允许时低权重跨 Race；自定义 geneClass 与结构/外观基因只在目标 Race 已观察或明确声明时允许。
- GenerateGenes Patch 加入 HarmonyAfter HAR；非 Human 必须 Humanlike、HAR 适配可用且确属 HAR Race。
- CanHaveGene 预检和 HAR AddGene Prefix 双重裁决；AddGene null 后换候选，每位点最多 20 次。
- 阶段 2 出生逻辑保持原版 Human 边界，未扩展 HAR 出生突变。

### 诊断、文档与元数据

- 新增 HAR 状态、扫描 Race、同 Race/跨 Race/Def 回退添加、CanHaveGene 拒绝、适配失败等聚合计数。
- 新增“只读查看第三阶段HAR适配器报告”“只读查看世界系谱池按种族摘要”，并把地图统计扩展为成人 Humanlike。
- 根据用户测试反馈新增“一键测试系谱基因四比二比一权重”：自动选择 HAR 种族、两个阵营定义和三个普通系谱基因，在内存中构造等量来源并直接用中文判定 40、20、10；取消手工准备三组 Pawn、生成 200 人和人工统计。
- About.xml 更新阶段 3 中英文说明；HAR 仅加入 loadAfter，不加入 modDependencies。
- 四件套、核心公约、AGENTS、阶段 3 白话测试/静态清单/游戏内记录已更新。

### 测试易用性全面重构

- 根据用户反馈，把第三阶段后半部分全部改为准确中文、逐按钮、无需猜测的测试步骤。
- 新增 `GD_Phase3AutomatedTests.cs`，提供 HAR 临时人物生成、限制检查、随机安全、异种类型限制、特殊基因边界、最终拒绝、缓存速度、阶段一/二回归、保存读取和移除结构等一键按钮。
- 原四比二比一手工统计、手动准备多组人物、人物编辑器依赖、数百人物批量生成和人工计算要求全部删除。
- 保存读取改为“准备记录→正常保存重载→一键验证”；通过后自动清理配置目录临时记录。
- 原有开发者按钮改用准确中文名称：成年人物、第三阶段、按种族摘要、第二阶段等。
### 第三阶段验收与测试按钮清理

- Agent 已读取用户提供的 `模组制作工作台/其他文档/日志.txt`，自动填写正式验收表并保存原始日志副本；用户无需复制表格或判断结论。
- 第三阶段总体通过：HAR 适配、当前世界系谱、四比二比一、HAR 最终拒绝、随机安全、异种类型限制、特殊基因边界、缓存、保存读取与移除安全结构均有通过证据。
- 已删除 `GD_Phase3AutomatedTests.cs`、四比二比一临时按钮和第二阶段列表隔离临时按钮。
- 最终只保留六个长期缓存/只读诊断按钮，避免开发者操作菜单堆积。
- 清理后重新 Release 构建并同步，DLL SHA-256：`E3B826924866267D9DE2D942C61783469126100F8F30F26146F9ABC7C09F12D0`。
- 已强化 `rimworld-mod-development` Skill 与 `references/一键测试方案模板.md`：每阶段先做测试项目到一键入口映射、能串行时提供阶段总按钮、日志解释每一步与验证理由、阶段记录完成后立即删除测试按钮。
- 已创建并更新 `模组制作工作台/其他文档/基因多样性模组阶段4至6一次性完成交接文档_2026-07-28.md`，要求新会话一次性完成剩余阶段 4～6。

### 验证状态

- Release 构建：0 warning、0 error。
- DLL 引用列表无 AlienRace 程序集；About.xml 可解析；源码无 HAR 静态类型引用与 AlienChanceEntry.Select/Approved 调用。
- `GD_BirthMutationPatch.cs`、`GD_BirthMutationUtility.cs`、`GD_MetabolismUtility.cs` SHA-256 与阶段 2 基线一致。
- 开发主副本 DLL SHA-256：`E3B826924866267D9DE2D942C61783469126100F8F30F26146F9ABC7C09F12D0`。
- 首次同步检测到 `RimWorldWin64` 正在运行，因此未擅自结束游戏；等待游戏正常退出后已自动完成白名单同步。
- 测试按钮清理后的开发/运行 DLL SHA-256 均为 `E3B826924866267D9DE2D942C61783469126100F8F30F26146F9ABC7C09F12D0`；运行副本仅含 About.xml 与 DLL。
- HAR 游戏内矩阵已由用户日志完成验收；两项日志证据边界已在正式验收记录中明确说明，未伪造缺失按钮块。

## 2026-07-28｜阶段 2 出生突变实现与核心公约固化

### 永久设计规则

- 新增根目录 `AGENTS.md` 与 `模组文档/核心设计公约.md`，把“当前存档已存在异种 Endogenes 永远优先，原版与 HAR 同等适用”固化为后续 Agent 的不可变约束。
- 同步更新阶段 2 交接文档、详细设计方案和创作之书索引；阶段 3 的首要目标明确为把当前存档 HAR Pawn Endogenes 纳入世界常见池。

### 阶段 2 实现

- 新增 `GD_BirthMutationPatch.cs`：精确绑定 RimWorld 1.6 的 `PregnancyUtility.ApplyBirthAutcome` 全参数签名，使用最后优先级 Prefix。
- 新增 `GD_BirthMutationUtility.cs`：对非 null GeneSet 出生只投一次 10% 骰子，从原版突变池最多尝试 20 个候选，成功时复制源列表并最多追加一个 Endogene。
- `genes == null`、非原版 Human 母亲、非空且非原版 Human 父亲全部安全跳过；不调用 `GetInheritedGenes`。
- 扩展 `GD_GeneSafetyValidator` 与 `GD_MetabolismUtility`，新增基于拟定 Endogene 列表的出生安全/覆盖后代谢入口，不伪造 Pawn。
- 扩展聚合诊断与两个阶段 2 只读开发者动作；正常模式不逐胎刷日志。
- `About.xml` 与加载日志更新为阶段 2。

### 构建与静态检查

- Release 构建：0 warning、0 error。
- 静态确认：阶段 1 Newborn 跳过保留；源码无 `GetInheritedGenes` 引用；出生逻辑不对源 `genes` 原地 Add/Remove/Clear；About.xml 可解析。
- 最终开发/运行 DLL SHA-256 均为 `8ADC84CD982C0B4BC7ACD4ADC48EDCFEC25C96878497B7388BB8D85FB09A0C0D`；运行副本仅含 About.xml 与 DLL。
- 阶段 2 游戏内自然出生、旧档怀孕、胚胎、成长舱、统计、保存读取与移除回滚仍待用户验收，未把构建成功描述为游戏内通过。

## 2026-07-28｜阶段 1 游戏内验收通过（用户反馈）

- 用户于 2026-07-28 明确反馈：“测试全部通过，无错误”。
- 据此将阶段 1 的游戏内验收状态更新为通过：最小加载、缓存 Debug Actions、普通 Pawn 生成、战斗/Violent 安全、保存读取、原版遗传、新生儿跳过、旧人物不回填、性能与日志均通过。
- 验收过程中未报告本模组红字、错误、生成耗尽、存档异常或明显性能问题。
- **结果来源：用户反馈；Agent 未在本会话亲自操作 RimWorld 游戏。** 本条仅记录用户完成测试后的验收结论，不伪称 Agent 直接观察。
- 详细记录见 `测试/阶段1游戏内验收记录_2026-07-28.md`。

## 2026-07-27｜根目录运行副本白名单同步

- 已创建并同步 `D:\steam\steamapps\common\RimWorld\Mods\yyyyy_GeneticDiversity`。
- 运行副本严格只包含 `About/About.xml` 与 `1.6/Assemblies/GeneticDiversity.dll`。
- Source、模组文档、测试、Wiki、构建脚本和中间产物均未进入运行副本。
- 运行副本已可供加载测试；RimWorld 游戏内项目仍待用户验证，未标记为通过。

## 2026-07-27｜主会话实现整合修正与最终重构建

### 实现整合

- `About.xml` 已将 Biotech 声明为硬依赖；取消无 Biotech 加载测试安排。
- 新增 `GD_MetabolismUtility.cs`：把当前 Xenogenes、Endogenes 与候选 Endogene 组装为 `GeneDefWithType`，通过 `NonAverriddenGenes()` 预模拟覆盖后的最终有效基因。
- 代谢和 Violent 禁用均按未被覆盖的最终有效基因计算，不再简单累计全部基因。
- 世界 Pawn 扫描在枚举前立即 `ToList()`，避免活集合变化导致枚举异常。
- 官方基因池额外过滤带 `bodyType` 或非空 `forcedHeadTypes` 的基因。
- Harmony Patch 显式指定 `PawnGenerator.GenerateGenes(Pawn, XenotypeDef, PawnGenerationRequest)` 参数签名，并显式标注 Postfix。
- ForcedEndogenes/ForcedXenogenes 改用 `NullArEmpty()` 判断，避免空列表被误判为强制请求。

### 构建修正

- 首次构建发现 `IntRange.Includes` 在当前引用中为 `internal`，模组程序集不能直接调用。
- 代谢范围检查改为比较 `GeneTuning.BiostatRange.min/max`。
- 最终 Release 重构建结果：**0 警告、0 错误**。
- 开发主副本 DLL 已输出到 `1.6/Assemblies/GeneticDiversity.dll`。

### 测试易用性全面重构

- 根据用户反馈，把第三阶段后半部分全部改为准确中文、逐按钮、无需猜测的测试步骤。
- 新增 `GD_Phase3AutomatedTests.cs`，提供 HAR 临时人物生成、限制检查、随机安全、异种类型限制、特殊基因边界、最终拒绝、缓存速度、阶段一/二回归、保存读取和移除结构等一键按钮。
- 原四比二比一手工统计、手动准备多组人物、人物编辑器依赖、数百人物批量生成和人工计算要求全部删除。
- 保存读取改为“准备记录→正常保存重载→一键验证”；通过后自动清理配置目录临时记录。
- 原有开发者按钮改用准确中文名称：成年人物、第三阶段、按种族摘要、第二阶段等。
### 第三阶段验收与测试按钮清理

- Agent 已读取用户提供的 `模组制作工作台/其他文档/日志.txt`，自动填写正式验收表并保存原始日志副本；用户无需复制表格或判断结论。
- 第三阶段总体通过：HAR 适配、当前世界系谱、四比二比一、HAR 最终拒绝、随机安全、异种类型限制、特殊基因边界、缓存、保存读取与移除安全结构均有通过证据。
- 已删除 `GD_Phase3AutomatedTests.cs`、四比二比一临时按钮和第二阶段列表隔离临时按钮。
- 最终只保留六个长期缓存/只读诊断按钮，避免开发者操作菜单堆积。
- 清理后重新 Release 构建并同步，DLL SHA-256：`E3B826924866267D9DE2D942C61783469126100F8F30F26146F9ABC7C09F12D0`。
### 验证状态

- 根目录运行副本已创建并完成白名单同步，仅含 About.xml 与 DLL；RimWorld 游戏内测试仍待用户验证。
- 四件套、白话测试方案与静态清单已按真实实现更新；游戏内项目待运行副本交付后由用户验证。

## 2026-07-27｜阶段 1 文档与测试资料

### 新增

- 创建四件套：`模组总文档.md`、`修改日志.md`、`模组修改注意事项.md`、`模组逐文件白话说明.md`。
- 创建 `测试/阶段1白话测试方案.md`。
- 创建 `测试/阶段1静态检查清单.md`。
- 固化阶段 1 边界：仅原版 Human/Xenotype，新生成非新生儿 Pawn 追加 0～3 个 Endogene 变体，每个位点 90% 常见池/10% 突变池。
- 固化安全检查：代谢、冲突、前置、发色、肤色、超凡、近亲与暴力能力。
- 固化跳过条件：ForcedCustomXenotype、非空 ForcedEndogenes、非空 ForcedXenogenes 与 Newborn。

### 范围限制

- 文档维护任务不修改 `About/`、`1.6/`、`Source/`、`Wiki/`。
- 该条目创建时未同步；后续已由主会话完成根目录运行副本白名单同步。




















## 阶段 6 收尾（2026-07-30）

本阶段把前五阶段的固定行为整理为可持久化设置，并补齐只读统计和旧档主动补充工具。默认值保持前五阶段行为：启用、玩家/NPC 均启用、标准强度、突变比例 10%、新生儿突变概率 10%、普通功能基因允许跨种族、特殊/外观/身体基因默认不跨种族、详细日志关闭、Archite 突变关闭。

旧档工具不会自动回填，也不直接修改存档 XML。玩家必须在游戏内主动点击并二次确认，建议先另存测试档。工具会跳过婴儿、任务专用人物和无基因追踪器人物，并在世界组件中按 `thingIDNumber` 记录已处理人物以防重复追加。移除模组前应保留备份；如需彻底清除处理记录，应在游戏内使用“清除旧档已处理记录”。

阶段 6 临时测试入口在用户日志判定完成前保留。


## 阶段6用户验收与最终收口（2026-07-30）

- 修复世界生成早期读取 `Faction.OfPlayer` 导致的 `Could not find player faction.`。
- 修复阶段6 Debug Action 和 ModSettings 分类的 Windows 代码页问号乱码。
- 用户新 Player.log 中阶段6总测试 7 项 PASS，目标模组关联红字 0。
- 验收后删除 `GD_Phase6AutomatedTests.cs` 和阶段6临时测试按钮，保留永久统计/旧档工具。
- 最终 DLL SHA-256：`7E60C929155D4C76C3D6026C5E4F19AB12AEC5DEDCD2EC8D5A24B0FBE69299E5`。


## 阶段6收口后 ModSettings 主菜单热修（2026-07-30）

- 修复主菜单打开模组设置时无条件读取 `Find.Selector` 导致的 `InvalidCastException`。
- 当游戏未处于 Playing 或没有 CurrentMap 时，不显示“当前选中 Pawn”强度覆盖入口；全局设置仍可正常使用。
- 修复 DLL SHA-256：`C6EB0C1D81D51784102F16CDBFB7CBE6007C68BF0780A903466A27D92BC60171`。
